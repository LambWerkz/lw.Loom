# lw.Loom
Named, persistent runspace pools for PowerShell. Create pools, pre-load modules, and dispatch async commands by pool ID with full $using: support. Built for non-blocking GUI/TUI workflows. No more frozen UIs. Works on PowerShell 5.1 and 7+.
